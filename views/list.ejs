
   <%- include("header") -%>
 
        <div class="box" id="heading">
            <h1><%= listTitle %></h1>
        </div>
        
       <div class="box">
       
            <!-- 2 ways for loop -->

            <% for(var i=0 ; i<newItem.length; i++) { %>
              <form class = "item" action="/delete" method="POST">
                <div class="item"> 
                  <input type="checkbox" name="check" value="<%= newItem[i]._id%>" onChange="this.form.submit()">
                <p><%= newItem[i].name %></p>
              </div>
              <input type="hidden" name="listName" value="<%= listTitle %>"></input>
              </form>
            <% } %>

            <!-- <%  newItem.forEach(function(newItem){ %>

              <form action="/delete" method="post">
                <div class="item">
                  <input type="checkbox" name="check" value="<%=newItem._id%>" onChange="this.form.submit()">
                  <p><%=newItem.name%></p>
                </div> 
              </form>
            <%  }) %> -->

            <form class="item" action="/" method="POST">
                <input type="text" name="item" placeholder="New Item" autocomplete="off" />
                <button type="submit" name="list" value="<%= listTitle %>">+</button>
            </form>
      
       </div>  

<%- include("footer") -%>